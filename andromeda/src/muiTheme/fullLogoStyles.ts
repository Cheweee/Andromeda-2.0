import { Theme } from "@material-ui/core";

export const fullLogoStyles = (theme: Theme) => ({
    blackColor: {
        color: 'rgb(0, 0, 0)'
    },
    text: {
        border: "none",
        outline: "none",
        fontSize: "inherit",
        lineHeight: "1em",
        padding:0,
        margin:0
    }
});